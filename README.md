```
Requirements
- GLFW
- CMake
- Glad
```

## How to run

```
cmake -S . -B cmake_data         # configure
cmake --build cmake_data         # build
cmake --build cmake_data --target run       # run the program
cmake --build cmake_data --target clean_all # clean up
```
